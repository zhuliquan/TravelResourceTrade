package com.manager.auctioner;

import java.util.ArrayList;

import com.market.environment.Agent;
import com.market.environment.Environment;
import com.market.environment.Item;

public class Auctioner {
	public Auctioner() {}
	public static void FindBuyer(Environment env) {
		ArrayList<Item> items = env.getItems();
		ArrayList<Agent> agents = env.getAgents();
		
	}
	public static void SecondSealAuction(Environment env) {
		
	}
}
